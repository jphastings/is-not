{
  "id": "at.isnot.lens.network-cosmik-card",
  "description": "A Cosmik card saves a web page or a note; the tag subject is that page or note.",
  "source": "network.cosmik.card",
  "target": "at.isnot.review#subject",
  "extensions": {
    "at.isnot": { "title": ["content.metadata.title", "content.url", "content.text"] }
  },
  "steps": [
    {
      "apply_expr": {
        "field": "type",
        "expr": "if type == \"URL\" then \"web-page\" else if type == \"NOTE\" then \"post\" else type",
        "coercion": "projection"
      }
    }
  ]
}
