{
  "id": "at.isnot.lens.fyi-atstore-listing-detail",
  "description": "A fyi.atstore.listing.detail record describes one app in the AT Store directory; the tag subject is that app.",
  "source": "fyi.atstore.listing.detail",
  "target": "at.isnot.review#subject",
  "steps": [
    { "rename_field": { "old": "name", "new": "title" } },
    { "add_field": { "name": "type", "kind": "string", "fallback": "app" } }
  ]
}
